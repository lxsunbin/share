import React, {Component} from 'react';

export default class Game extends Component{
  constructor(...args){
    super(...args);
  }

  render(){
    return (
      <div>
        游戏
      </div>
    );
  }
}
